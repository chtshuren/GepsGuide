# AGENTS.md

## 專案目的
製作一個簡易的靜態網頁,導覽「政府電子採購網」的重要功能,供使用者快速了解如何操作。

## 技術棧
- 純 HTML/CSS/JS,不使用前端框架或建置工具(no bundler/build step)。
- 產出的網頁應可直接以瀏覽器開啟或部署到任何靜態主機,無需額外編譯。

## 目錄結構與慣例
- `/source`:原始素材,包含政府電子採購網的操作截圖(圖片)與對應的文字說明/腳本。修改網頁內容前,先確認 `/source` 內是否已有對應素材;新增功能導覽前,先在此補齊素材再製作頁面。
- `/target`:最終產出的靜態網頁(HTML/CSS/JS 及引用的圖片)。此目錄是唯一會被部署/發布的內容,勿在此目錄外放置需要上線的檔案。
- 素材與產出為手動對應,沒有自動化建置流程 —— 修改 `/source` 素材後需手動更新 `/target` 內對應頁面與圖片。

## 開發慣例
- 每個「重要功能」建議對應一個獨立頁面或頁面內一個章節,並搭配該功能的截圖與簡短文字說明。
- 圖片檔名建議使用有意義的英文/拼音命名並與 `/source` 內原始檔名保持可追溯的對應關係。
- 頁面文字以繁體中文撰寫,與截圖對象(政府電子採購網)語言一致。

## 素材命名規則與新增流程
- `/source` 內每個功能對應一組檔案:`N.功能名稱.png`(截圖)+ `N.功能名稱_desc.md`(第一行中文說明、第二行對應英文翻譯)。
- 當 `/source` 新增一組 `N.功能名稱.png` + `N.功能名稱_desc.md` 時,於 `/target/index.html` 依同樣模式加入:
  1. 於 `<nav class="toc">` 加入一個指向該功能區塊的導覽連結(`<a href="#錨點">`)。
  2. 於 `<main>` 新增一個 `<section class="feature" id="錨點">`,包含標題、`images/` 下對應截圖,以及中英文 `desc-block` 說明區塊。
  3. 將截圖從 `/source` 複製一份到 `/target/images/`,檔名與 `/source` 保持一致。
- `/source` 內每個說明 `.md` 都必須保留成對的中文與英文內容；若目前只有中文,修改來源檔時需先補上對應英文,再同步更新 `index.html`,不可只在 `index.html` 補英文。
- `.md` 若有更新,需同步更新 `index.html` 對應區塊的中英文文字,不可只改單一語言。
- `index.html` 中的說明文字與 `/source` 對應 `.md` 的內容及換行結構必須一致；`.md` 中的換行、分段或 `<br />` 對應格式不可在同步時省略或自行合併。
- `/source` 內的 `.md` 說明文字若包含 Markdown 粗體語法 `**xxx**`,產生或更新 `index.html` 時需將 `xxx` 轉為粗體且紅色顯示,不論內容使用中文、英文或其他語言；請使用具語意的 HTML 標記(例如 `<strong class="emphasis-red">xxx</strong>`)並確保 `target/styles.css` 為該標記設定紅色與粗體樣式,不可在頁面上顯示 `**` 符號。

## 指令約定
- `/檢查md`: 檢查 `/source` 內所有 `.md` 是否都有成對的中文與英文內容；若缺少英文,列出檔案與需要翻譯的內容,不可直接只修改 `index.html`。
- `/更新html`: 將 `/source` 中新增或更新的圖片與 `.md` 內容同步補進 `/target/index.html` 及 `/target/images/`,並確認頁面內容、雙語文字與換行格式正確；完成後提供可供使用者確認的頁面結果。此指令不包含 commit 或 push。
- `/更新page`: 在完成 `/更新html` 並確認頁面正常後,允許建立 commit,並將更新推送到 `main` 與 `gh-pages` 分支。推送前需確認變更內容與目標分支,不可將未確認的素材或頁面變更直接發布。

## 多語系需求
- 此網頁的目的是讓**非中文使用者**也能看懂如何操作政府電子採購網,因此每個頁面/章節的說明文字都須提供英文版本。
- 建議中英文並列呈現(例如中文段落後緊接對應英文翻譯,或提供語言切換),避免只放中文而讓非中文使用者無法理解。
- 新增或修改任何說明文字時,務必同步補上對應的英文翻譯,不可只更新其中一種語言。
- 若說明文字中包含網址,務必以超連結形式呈現(`<a href="...">`)而不是純文字,並保留在中英雙語區塊中同步更新。
- `_desc.md` 內容可換行,但中文與英文敘述需成對保留在同一個功能區塊內,勿只保留單一語言或把語句拆離。
- `span.lang-tag` 建議獨立一行顯示,讓中英文雙語區塊更容易閱讀與維護。
